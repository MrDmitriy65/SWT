pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Repo for swipe
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Serbian Words Trainer"
include ':app'
